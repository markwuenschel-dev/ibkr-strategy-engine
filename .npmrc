# Refuse to run under a Node or pnpm older than package.json's `engines`.
# A silent "works on my machine" divergence is exactly what pinning the
# toolchain is meant to prevent.
engine-strict=true

# This package has zero dependencies and is never published.
# Keep pnpm from reaching for a registry it does not need.
resolution-mode=highest
